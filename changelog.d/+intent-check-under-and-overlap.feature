`decree intent-check` gained a first-class `--under` view — additive `owned_files`, `contextual_overlaps`, and `contradictions` keys that frame multi-governed paths around the active decision — plus an advisory `directory_overlaps` list that surfaces co-governance via directory `governs:` entries the exact-path conflict query cannot see. Both are additive and never change the exit code.
