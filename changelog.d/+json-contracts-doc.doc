Add `docs/json-contracts.md`: the reference for programmatic consumers — the `--json` stdout/stderr split, the **exit-code contract** (0 = clean, 1 = findings with JSON still on stdout, 2 = hard error), the `decree.error.v1` shape, and per-command payload keys. Also document copy-paste recipes for gating decree in a consumer repo (hand-rolled git hook, `pre-commit` framework, and CI) in `docs/usage.md`.
