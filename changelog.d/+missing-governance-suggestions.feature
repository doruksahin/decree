`decree health` now reports **suggested governance** (advisory) — files a decision's own trailer-linked commits repeat-touch (≥2 distinct commits) but that it does not declare in `governs:` and no decision owns, surfaced as "ungoverned files with a proposed owner." It is the inverse of dead-governance and deliberately lower-authority: it never affects `decree health`'s exit status and is never read by `why()`. Precision rests on per-decision repeat-touch attribution rather than cross-decision frequency (which squash commits defeat), with a shared-infrastructure floor and deterministic, path-based filtering (no working-tree reads).
