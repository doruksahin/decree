Add `decree commit-check`: a deterministic trailer-coverage gate (and matching MCP tool) that reports which governed-file changes in a diff lack an `Implements:/Refs:/Fixes:` trailer linking them to their in-flight decision. Advisory by default; `--strict`/`--min-coverage` gate CI on the net diff (squash-safe via `--diff-base`). Reads only declared `governs:`; coverage you can gate, not a guarantee.
