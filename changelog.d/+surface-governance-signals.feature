The MCP `health` tool and `decree ddd` now surface the governance-drift signals — dead governance (SPEC-01KT6EEAHKWDQB2Y6S4TTKB77D) and advisory suggested governance (SPEC-01KT6NCQC7DMJ3NG1MPWBGBVFQ). The MCP `health` payload is serialized through the same formatter as `decree health --json`, so the two never diverge and agents receive both signals over MCP. `decree ddd` reports per-corpus dead/suggested-governance counts as a lifecycle hint — a fail-safe, pure index read (no git walk; zero, never an error, when there is no index). `decree lint` is deliberately left untouched: it validates document structure and never reads the index, and the codebase keeps validation separate from coherence.
