`decree intent-check` and `decree intent-review` accept `--under <decision>` (and an `under` parameter on the matching MCP tools). When a governed session passes the decision it works under, the report surfaces `governs_gaps` — planned/changed files that decision's own trailer-linked commits repeat-touch (>=2 commits, squash-immune) but it does not declare — plus a soft `declare_governs` recommendation. It is the point-of-change counterpart to batch missing-governance (`decree health`): scoped to a known decision and a live edit, it drops the cross-decision filters the batch surface needs, so it surfaces decision-specific gaps the batch surface suppresses. Advisory only — it never changes the exit code, never feeds `why()`, and never writes; the agent makes the deliberate `governs:` edit. An unknown `--under` id exits 2.
