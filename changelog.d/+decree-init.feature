Add `decree init`: a deterministic, idempotent project scaffolder that creates a canonical `decree.toml`, the type directories, a worked PRD→ADR→SPEC example chain, a `.gitignore` rule for the derived `.decree/` cache, and a built index — reporting every action with a reason (created / skipped / wrote / appended / rebuilt). It respects an existing `decree.toml`: it leaves it untouched and scaffolds *its* declared types rather than imposing the default trio (no orphan directories in a custom corpus), and reports a malformed config clearly without touching it. The report is clean and ordered: the derived index cache is reported as *rebuilt* (never counted as a creation), and a re-run on a set-up project prints `Already initialized — nothing to create (index refreshed).`. Flags: `--dry-run`, `--json`, `--no-examples`, and `--project`. The scaffolded project lints clean immediately.
