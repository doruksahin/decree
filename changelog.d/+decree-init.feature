Add `decree init`: a deterministic, idempotent project scaffolder that creates a canonical `decree.toml`, the type directories, a worked PRD→ADR→SPEC example chain, and a built index — reporting every action with a reason (created / skipped / rebuilt). The report is clean and ordered: the derived index cache is reported as *rebuilt* (never counted as a creation), and a re-run on a set-up project prints `Already initialized — nothing to create (index refreshed).`. Flags: `--dry-run`, `--json`, `--no-examples`, and `--project`. The scaffolded project lints clean immediately.
